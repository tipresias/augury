# Only contains the dependencies necessary to run the serverless functions.
# This reduces the file size deployed to Google Cloud.

requests
google-cloud-storage==2.1.0
joblib
simplejson
bottle==0.12.19
gunicorn
rollbar

# Data packages
numpy==1.22.1
pandas==1.4.0
scikit-learn==1.0
xgboost==1.5.2
mlxtend==0.19.0
statsmodels==0.13.1
scipy==1.8.0
tensorflow==2.8.0
scikeras==0.4.1
six~=1.15.0 # Required version for TF 2.4.1

# Kedro packages
kedro==0.17.0
gcsfs<2022.2 # Needed to access Google Cloud Storage files
fsspec<2022.2 # Needed for DataSet functionality
MarkupSafe<2.0.0 # Required version for 'cookiecutter' (a 'kedro' dependency)
Jinja2<3.0.0 # Required version for 'cookiecutter' (a 'kedro' dependency)

# Testing/Linting
mypy==0.931 # Need mypy due to references to mypy_extensions in production code
